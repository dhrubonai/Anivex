# Cloudflare Pages Configuration
# Deploy with: npx wrangler pages deploy out --project-name anivex
# Or connect your GitHub repo in Cloudflare dashboard

name = "anivex"
compatibility_date = "2024-01-01"

[site]
bucket = "./out"

# Cloudflare Pages Functions are auto-detected from /functions directory
# functions/api/anime-servers.js → /api/anime-servers
